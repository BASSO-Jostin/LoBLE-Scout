CONFIG_SERIAL=y          # UART support
CONFIG_GPIO=y            # GPIO support


# Use minimal libc instead of picolibc to avoid lock conflicts
CONFIG_MINIMAL_LIBC=y
CONFIG_NEWLIB_LIBC=n
CONFIG_PICOLIBC=n
